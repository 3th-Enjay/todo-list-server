const path = require('path');
const fs = require('fs');
const baseDir = path.join(__dirname, '../to-dos');


class todoService {

    createTodo(name, data) {
        const fileName = path.join(baseDir, name)
    }
}